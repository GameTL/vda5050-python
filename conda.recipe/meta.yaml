package:
  name: vda5050
  version: 0.0.1

source:
  path: ..

build:
  number: 0
  script:
    - unset CMAKE_GENERATOR && {{ PYTHON }} -m pip install . -vv  # [not win]
    - {{ PYTHON }} -m pip install . -vv  # [win]

requirements:
  build:
    - python
    - {{ compiler('cxx') }}

  host:
    - cmake
    - ninja
    - python
    - pip
    - scikit-build-core
    - pybind11 >=2.10.0

  run:
    - python
    - rich


test:
  imports:
    - vda5050
  requires:
    - pytest
  source_files:
    - tests
  commands:
    - pytest tests

about:
  summary: VDA5050 Python bindings built with pybind11 and scikit-build-core.
  license_file: LICENSE
